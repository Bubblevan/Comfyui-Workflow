# Nun example

The reusable manifests live in `characters/nun.yaml` and
`shots/nun/shot01_idle.yaml`. Copy those files as a starting point for a new
character or shot; the harness does not special-case `nun`.
